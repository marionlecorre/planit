<div class="modal fade addExpenseModal" id="basicModal" tabindex="-1" role="dialog" aria-labelledby="basicModal" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
            <button id="close-modal" type="button" class="close" data-dismiss="modal" aria-hidden="true"></button>
            <h4 class="modal-title" id="myModalLabel">{{'add_item'|trans}}</h4>
            </div>
            <div class="modal-body">
                <form id="form-post-expense" action="" method="post" {{ form_enctype(form) }}>
                    {{ form_errors(form) }}
                    {{ form_widget(form) }}

			    <p>
			        <input type="submit" value="{{'add'|trans}}">
			    </p>
                </form>
            </div>
        </div>
  </div>
</div>
